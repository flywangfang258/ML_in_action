bayes:
优点：在数据较少的情况下仍然有效，可以处理多类别问题
缺点：对于输入数据的准备方式比较敏感
适用数据类型：标称型数据

P(ci|x,y)= [P(x,y|ci)*P(ci)] / P(x,y)

（1）收集数据：可以用任何方法。 本章使用RSS源
（2）准备数据：需要数值型或者布尔型数据
（3）分析数据：有大量特征时，绘制特征作用不大，此时使用直方图效果最好
（4）训练算法：计算不同的独立特征的条件概率
（5）测试算法：计算错误率
（6）使用算法：一个常见的朴素贝叶斯应用是文档分类。可以在任意地分类场景中使用朴素贝叶斯，不一定非要是文本。